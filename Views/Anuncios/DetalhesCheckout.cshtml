@model CliCarProject.Models.Classes.Anuncio

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card border-0 shadow-lg rounded-4 overflow-hidden">
                <div class="bg-primary p-4 text-white text-center">
                    <h3 class="fw-bold mb-0"><i class="bi bi-cart-check me-2"></i>Finalizar Compra</h3>
                    <p class="mb-0 opacity-75">Simulação de Checkout - Fase 3</p>
                </div>
                <div class="card-body p-5">
                    <div class="row align-items-center mb-4">
                        <div class="col-md-4">
                            <img src="@(Model.IdVeiculoNavigation?.Imagems?.Any() == true ? Url.Content($"~/uploads/veiculos/{Model.IdVeiculo}/{Model.IdVeiculoNavigation.Imagems.First().Nome}") : "/images/no-image.png")"
                                 class="img-fluid rounded-3 shadow-sm">
                        </div>
                        <div class="col-md-8">
                            <h4 class="fw-bold text-dark">@Model.Titulo</h4>
                            <p class="text-muted">Estado do Anúncio: <span class="badge bg-info text-dark">Reservado</span></p>
                        </div>
                    </div>

                    <div class="bg-light p-4 rounded-4 mb-4">
                        <div class="d-flex justify-content-between mb-2">
                            <span class="text-secondary">Preço do Veículo:</span>
                            <span class="fw-bold">@Model.Preco.ToString("N2") €</span>
                        </div>
                        <div class="d-flex justify-content-between mb-2">
                            <span class="text-secondary">Taxas de Serviço:</span>
                            <span class="text-success fw-bold">Grátis (Simulação)</span>
                        </div>
                        <hr>
                        <div class="d-flex justify-content-between">
                            <h4 class="fw-bold">Total a Pagar:</h4>
                            <h4 class="fw-bold text-primary">@Model.Preco.ToString("N2") €</h4>
                        </div>
                    </div>

                    <div class="alert alert-warning border-0 rounded-4 d-flex align-items-center">
                        <i class="bi bi-info-circle-fill fs-4 me-3"></i>
                        <small>Ao confirmar, será gerada uma encomenda com o estado <b>Pendente</b>. Deverá simular o pagamento no histórico para concluir.</small>
                    </div>

                    <form asp-action="Checkout" method="post">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="idAnuncio" value="@Model.IdAnuncio" />
                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-primary btn-lg rounded-pill fw-bold py-3 shadow">
                                Confirmar Registo da Encomenda
                            </button>
                            <a asp-action="MinhasReservas" class="btn btn-link text-muted">Cancelar e Voltar</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>